import { ServerCallInterceptor } from './server-call-interceptor';

describe('ServerCallInterceptor', () => {
  it('should create an instance', () => {
    expect(new ServerCallInterceptor()).toBeTruthy();
  });
});
