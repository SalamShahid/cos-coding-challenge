import { BasicVehicleInformation } from './basic-vehicle-information';

describe('BasicVehicleInformation', () => {
  it('should create an instance', () => {
    expect(new BasicVehicleInformation()).toBeTruthy();
  });
});
